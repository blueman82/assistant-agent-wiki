# Log

## [2026-06-07] init | bootstrapped wiki with 5 seed pages

## [2026-06-29] ingest | wire-in-slack: new capabilities/slack + sources/2026-06-29-wire-in-slack; updated mcp-integrations, extending-system-md, index

## [2026-06-29] edit | scrubbed dead .env.ketchup references from slack pages (token file deleted, slack skill rewritten to MCP)

## [2026-07-07] ingest | new capabilities/send-gate documenting the PreToolUse enforcement hook (secretary.ts + gate/sendGate.ts); updated index. wiki-lint pass: no contradictions, no orphans, no stale (90+ day) pages found

## [2026-07-07] ingest | Telegram front-end post-merge docs pass (assistant-agent PR #9, docs/telegram-frontend, merged 7e885ad): new capabilities/telegram-frontend documenting bridge/telegram-bridge.ts (owns the single getUpdates loop, FIFO chat queue, single-user gating, /reset /status /stop bridge commands, launchd persistence); updated capabilities/send-gate with the update-routing split (bridge owns getUpdates, gate/surfaces/telegram.ts only consumes injected callback_query updates); updated index

## [2026-07-08] ingest | Telegram reply formatting fix (assistant-agent PR #14, fix/telegram-reply-formatting, merged 4a70688): updated capabilities/telegram-frontend with the stripMarkdown sanitiser (bridge/api.ts, wired into sendChunked pre-chunk) and the new prompts/system.md "Plain text replies" ground rule; documented the parse_mode/JSON-envelope rejection rationale (malformed entity → HTTP 400 → lost message; deterministic stripping can't lose a message); new sources/2026-07-08-telegram-reply-formatting.md. Lint pass: no contradictions found (grep for markdown/parse_mode/plain text across capabilities+architecture+patterns turned up only unrelated hits — tasks.md's own file-format note and extending-system-md's description of prompts/system.md as a markdown file)

## [2026-07-08] ingest | Rachel rebrand (assistant-agent PR #15, feat/rachel-rebrand, merged 3b871f7): secretary → Rachel rename, naming only, no behaviour change. Updated architecture/overview, architecture/mcp-integrations, capabilities/send-gate, capabilities/slack, capabilities/tasks, capabilities/telegram-frontend, patterns/extending-system-md, sources/2026-06-29-wire-in-slack, index — secretary.ts→rachel.ts, SECRETARY_*→RACHEL_*, ~/.secretary/→~/.rachel/, com.secretary.telegram-bridge→com.rachel.telegram-bridge; new sources/2026-07-08-rachel-rebrand.md. npm package name "assistant-agent" unchanged, left alone. Historical entries above this line still say "secretary" — append-only log, left as-is, accurate to the time they were written. Lint pass: post-rename grep for "secretary" (case-insensitive, excluding raw/ and log.md's own history) returns zero hits; "Rachel" appears in all 9 touched pages; no orphans (new source page linked from index); no dead links introduced

## [2026-07-08] ingest | Telegram typed emit channel (assistant-agent PR #16, fix/telegram-emit-channel, merged 6552813): rachel.ts's runTurn now tags each emitted line with a TurnEmitKind ("text" | "tool" | "meta"); bridge/telegram-bridge.ts filters to kind==="text" only, keeping tool-use echoes and the "[Rachel] done turns=" footer off Gary's phone while the terminal REPL still prints every kind. Updated capabilities/telegram-frontend (new "Reply-content contract" section) and architecture/overview (brief TurnEmit/TurnEmitKind note); new sources/2026-07-08-telegram-emit-channel.md documenting the review-driven classification-pinning test (eval N2, independent verifier confirmed structural not regex-based filtering) and that PR #15's rebrand merged mid-flight and was reconciled into the frozen evals (amendments array, negative controls deliberately left unamended against the pre-rename base). Lint pass: grep for "secretary"/"SECRETARY_"/"com.secretary"/"~/.secretary" across the vault (excluding raw/) found only the two pages already marked historical-and-intentional (this log's own append-only entries, and sources/2026-07-08-rachel-rebrand.md's description of the pre-rename name) — no new staleness introduced; no orphans (new page linked from index); index updated

## [2026-07-08] ingest | Telegram image reception (assistant-agent PR #17, feat/telegram-image-reception, merged fdbd217): Rachel can now receive photos and image documents via Telegram. Updated capabilities/telegram-frontend (new "Image reception" section: photo selection, download flow, FIFO format, system.md contract, known debt); new sources/2026-07-08-telegram-image-reception.md. Key decisions captured: getFileUrl collapsed into downloadFile to prevent token leakage, 20MB guard added, PDF scope dropped, non-image docs get user-facing reply. Known debt: no temp file cleanup, no download timeout/AbortSignal. index updated.

## [2026-07-14] ingest | Terminal tool-echo truncation removed (rachel.ts, assistant-agent PR #20, merged 9e54423): the two .slice(0, 100) caps on tool-use summary lines (Bash command echo + JSON.stringify of other tool inputs) removed after Gary hit truncated terminal output. Terminal-only impact — the Telegram bridge filters emit to kind==="text" so tool echoes never reached the phone anyway. Bridge slice calls confirmed as chunking/sanitising, not truncation, left alone. Verified: typecheck clean, npm test 107/107 + probe suite pass. Updated architecture/overview (tool-echo bullet); new sources/2026-07-14-terminal-tool-echo-truncation.md; index updated.

## [2026-07-14] ingest | Bridge self-monitoring cluster (assistant-agent PR #21 merge 0598e4e + PR #22 merge 097d6c3, both merged to origin/main): the bridge's crash-loop `process.exit(1)`-on-first-409 replaced with a three-state health machine (healthy|conflict|failed) — a 409 now backs off CONFLICT_BACKOFF_MS (65s) and retries, exiting only after CONFLICT_EXIT_THRESHOLD (5) consecutive 409s (~5 min); transition-only alerts (never per-error); /status reports health + last error (recovered|ongoing); launchd ThrottleInterval:60; injectable conflictBackoffMs. PR #22 adds (a) tg() fetch timeout via AbortSignal.timeout(requestTimeoutMs ?? 45_000) so a wedged long-poll surfaces as an observable failed-state alert instead of an invisible hang, and (b) a best-effort "Rachel bridge started." startup alert so a non-409 crash-restart loop leaves a signal. CORRECTED stale content in capabilities/telegram-frontend.md: the launchd/KeepAlive line (~41) and the "One consumer only" bullet (~92) both described the removed exit(1)-on-first-409 behaviour — rewritten to the backoff/threshold-exit model. Added new "Self-monitoring (PR #21 + #22)" section to telegram-frontend.md with the WHY behind each piece; bumped its last_updated + tags. New sources/2026-07-14-bridge-self-monitoring.md. Also corrected the PR #20 log entry above from "not yet merged" to merged (9e54423). Verified all constants/behaviour against git show origin/main. index updated.

## [2026-07-14] lint | Health-checked the vault (17 pages). Fixed 2 items: (1) contradiction — capabilities/send-gate.md gave the dashboard approval-queue path as `~/.claude/coderails-dashboard/queue/<hash>.json`; code (`gate/surfaces/queue.ts` `DEFAULT_QUEUE_DIR`) and AGENTS.md both say `.../approvals/` — corrected to `approvals/`, added queue.ts to sources, bumped last_updated. (2) stale fact — capabilities/tasks.md claimed `tasks/` is "empty, only a .gitkeep"; two live task files now exist (launch-model-routing-loop.md, salience-cue-experiment-readout.md) — corrected + noted they don't follow the documented YYYY-MM-DD-slug naming; bumped last_updated. No orphans (every page reachable from index; email/calendar index entries are sanctioned "Not yet documented." gaps per AGENTS.md, not dead links). No stale pages (AGENTS.md governs at 90+ days for active-code pages; oldest is karpathy at ~37d, a stable external reference — exempt). Cross-reference sweep clean. Reported data gap (headline): capabilities/email + capabilities/calendar are core routed surfaces with no page — ingest-scale, out of lint scope.

## [2026-07-14] ingest | Inbox Brief (assistant-agent PR #23, feature/inbox-brief, merged fb6f675): recommend-only Gmail sweep — classifies recent mail (needs-action/new/noise), recommends actions (reply/archive/unsubscribe/ignore), never sends/unsubscribes/deletes/archives without being asked. New capabilities/inbox-brief.md + sources/2026-07-14-inbox-brief.md. Key new component: bridge/notify.ts, a standalone Telegram sender (reuses bridge/api.ts's sendChunked) for headless one-shot bin/rachel runs (launchd job com.rachel.inbox-brief — verified installed + loaded via launchctl list at ingest time; also a dashboard button per prompts/system.md, not independently verified live) that have no inbound bridge loop and thus no ordinary reply path to Telegram. notify.ts is deliberately ungated by the send gate (operator's-own-chat only, no destination arg) and reads its message from a file rather than argv specifically to avoid tripping gate/bashPatterns.ts's Bash send-pattern block on Rachel's own invocation of the script. Corrected the team-lead brief's framing: recommend-only is enforced by instruction, not solely by tool absence — the Gmail connector does expose unlabel_thread/unlabel_message, and removing INBOX is functionally archiving. Updated capabilities/telegram-frontend.md (new section distinguishing notify.ts's standalone path from the bridge's own reply path), capabilities/send-gate.md (new ungated-tool bullet + relationship), architecture/overview.md (headless one-shot execution mode), index.md. Lint pass on touched content only: no contradictions (notify.ts framed consistently as separate-from-bridge across all 3 updated pages); no orphans (both new pages linked from index); capabilities/email + capabilities/calendar remain the pre-existing "Not yet documented." gaps — PR #23 is a recommend-only sweep, not general Gmail surface documentation, so left as-is per scope (surfaced, not fixed, per AGENTS.md's ingest-vs-lint boundary).

## [2026-07-15] ingest | Inbox-brief `BRIEF FILE:` sentinel (PR #27, feature branch merged ef2783c): one line added to tasks/inbox-brief.md — once delivery is confirmed (exit 0 + `[notify] sent.`), Rachel's turn must end with a single machine-findable line, exactly `BRIEF FILE: <path>`, naming the scratch file she wrote and sent. Not printed on delivery failure or a skipped sweep. Exists to fix the coderails dashboard's INBOX BRIEF button, which previously showed Rachel's raw execution trace in the run-output modal instead of the actual brief — paired with a coderails-side change (PR #183, examples/dashboard-config.json) rewriting the button's command so the OUTER spawned claude agent reads the named file and makes its entire final message that file's verbatim contents; the sentinel alone couldn't reach the modal because it renders the outer agent's `result` field, a separate LLM layer from Rachel. Verified live: run 5cfc5ee0d5a32a82's modal output byte-matched the sent brief file, whitespace-normalised. Updated capabilities/inbox-brief.md (new "BRIEF FILE: sentinel" section, frontmatter last_updated+tags); no new source page (1-line diff, fully covered by the capability-page section + coderails-wiki's cross-repo source page pr_181_183_dashboard-run-output-wrap-and-inbox-brief-clean-modal.md). index.md unchanged (inbox-brief entry's existing description still accurate; no new page to list).

## [2026-07-15] ingest | Proactive layer cluster (assistant-agent PRs #24 merge 6f6e185, #25 merge 64232cc, #26 merge c387db3, #28 merge 9695a73; #27 merge ef2783c already ingested separately — all merge SHAs verified against origin/main via gh pr view). Rachel is now proactive: NEW capabilities/proactive-layer.md (push.ts chokepoint — one-ping-per-state dedup, quiet hours 22:30–08:00 Europe/Dublin defer-never-drop, urgent/normal/digest severities, 10/day budget, store layout at ~/.rachel/proactive/ with corruption policies; sweep families PR-red/bridge-liveness/calendar-<2h-escalation/producer-silence with fixed tick order and self-alert escalation; the closed liveness boundary with honest residuals; one-shot pattern with RACHEL_ALLOWED_TOOLS narrow-never-add narrowing, treat-as-data guardrails, message-from-file-never-argv; security invariants; live deploy state). NEW sources/2026-07-15-proactive-layer.md (per-PR breakdown, design provenance Hybrid D, live SO-2 verification, ToolSearch-survives-narrowing caveat flagged as deploy-session observation). CORRECTED now-stale claims: capabilities/telegram-frontend.md's startup-alert paragraph said sendChunked-direct and "next boot is the only signal a non-409 death leaves" — reworked to chokepoint-routed pushAlert (bridge-startup/bridge-health/loop-watchdog families, direct-send fallback, FATAL 409 path unchanged) + new heartbeat section + new "External liveness watch — boundary closed" section; sources/2026-07-14-bridge-self-monitoring.md given an explicit partially-superseded-by-PR-#26 banner (wedged-alive and launchd death now externally detected). capabilities/inbox-brief.md updated to the PR #28 evolution (six-tier taxonomy → severity mapping table, mail:<threadId> event-ids with <tier>:<latest-message-id> re-arming states, double-delivery exclusion, Gmail-only tool narrowing, 08:05 schedule correction from 08:00, injection guardrails). architecture/overview.md: one-shot narrowing + proactive layer note + RACHEL_ALLOWED_TOOLS config row. index.md updated (new capability + source entries, refreshed telegram-frontend/inbox-brief descriptions).

## [2026-07-15] lint | Post-ingest lint of the proactive-layer cluster (PRs #24–#26, #28), 19 pages. Fixed 5: (1) sources/2026-07-14-inbox-brief.md presented the pre-#28 state as current (08:00 schedule, single batch brief, no per-thread pushes) with no supersession marker — added a partially-superseded-by-PR-#28 banner parallel to the one bridge-self-monitoring got for #26, noting the v1 limitation is narrowed (urgent mail now interrupts) but not closed (still no mail producer-silence detection); bumped last_updated. (2) capabilities/send-gate.md never mentioned proactive/push.ts, while proactive-layer.md and the cluster source page both cite it as documenting why "push.ts/notify.ts" are ungated — extended the ungated bullet to cover both senders (no-destination + message-from-file invariants, test-pinned), added proactive-layer relationship + push.ts source, bumped last_updated; also fixed its stale "block below" → "above" pointer. (3) capabilities/tasks.md's folder-contents claim was stale — tasks/ now also holds one-shot prompt files (inbox-brief.md, proactive-calendar.md) and 3 launchd plists (verified against live listing); added a dual-purpose bullet distinguishing job instructions from Gary-task files. (4) architecture/overview.md said "PRs #24–#28", silently sweeping in the unrelated #27 sentinel — corrected to "#24–#26 and #28". (5) capabilities/inbox-brief.md's cross-repo wikilink [[pr_181_183_...]] can never resolve in this vault — converted to an explicit coderails-wiki path reference (target verified to exist there). Checked and clean: no ⚠️ flags; no orphans (all 19 non-template pages reachable from index); cluster numeric claims consistent everywhere (quiet hours 22:30–08:00, 10/day budget, 65s/threshold-5, 10-min wedge, 30-min stall, 48h grace, tick order, 08:05 vs 08 schedules); telegram-frontend's downloadFile no-timeout debt claim re-verified against bridge/api.ts (PR #22's AbortSignal is on tg() only — claim still true). Left as-is: [[capabilities/email]]/[[capabilities/calendar]] sanctioned gaps (index + mcp-integrations); raw/ empty, no inbox backlog; no 90+day stale pages.

## [2026-07-15] ingest | One-package installer (assistant-agent PRs #30-#32, merged + live-verified same evening): scripts/install.sh replaces all manual plist installation — stamps/installs/verifies the four com.rachel.* services, Telegram-config preflight mirroring loadTelegramConfig, proactive config bootstrap-if-absent-never-overwrite, truthful failure accounting. PR #30 hotfixed a push.ts CLI TDZ crash (guard-above-consts, latent until config.json exists). PR #32 fixed a live-found launchd race: bootout is async, immediate bootstrap gets exit 5 — teardown-wait added; the first live run failed HONESTLY (named all casualties) which is the design working. Live E9: blind clean-slate cycle passed; loop evals 9/9 GO. New capabilities/installation + sources/2026-07-15-installer; telegram-frontend persistence section now points at the installer; index updated.
## [2026-07-16] query-file-back | Runtime model/effort switching plan assumptions vs enforced code (RACHEL_MODEL constant-vs-per-turn asymmetry, multi-process state, /status staleness)
## [2026-07-18] query-file-back | Telegram voice STT+TTS spec (docs/coderails/specs/telegram-voice-stt-tts.md) cross-checked against bridge architecture: no prior wiki content on voice (confirmed clean), the 182d03e1 loop-retro non-goal is a session transcript not a wiki page (nothing to reconcile), FIFO shape migration confirmed against current code, 7 spec gaps found (drainFifo reply-branch not spelled out, no markdown-stripping before TTS, no output length cap, no spoken-register signal to the model, no install.sh preflight for new Python/ffmpeg deps, half-accurate execFile-timeout precedent, compounds already-filed temp-file cleanup debt). New investigations/2026-07-18-telegram-voice-stt-tts-spec-gaps.md; index updated.

## [2026-07-18] ingest | Drop the 1000-char cap on voice replies (assistant-agent PR #42, merged d476555): Task 8's outbound voice branch had invented `VOICE_REPLY_CHAR_LIMIT = 1000` — an unsourced constant, absent from the spec and this wiki, with a test pinning it as if it were a requirement. It surfaced as a user-visible bug: voice appeared to "work once, then stop" (short first reply spoke, longer second reply silently became text). Gary's call: drop the cap entirely — voice-origin turns now always answer in voice regardless of length; text is the fallback only when the synthesize/convert/send pipeline actually throws. Updated capabilities/telegram-frontend.md with a new "Voice in/out — STT+TTS" section covering the full feature (Tasks 4, 6-9) for the first time — inbound download+transcribe+FIFO-tag, outbound stripMarkdown+synthesize+convert+send with its all-or-nothing fallback, the cap's removal, and two now-live risks this exposes: no split-into-multiple-voice-messages story for long replies, and `sendVoiceFn` unchecked against Telegram's voice-message size ceiling (both inferred failure modes, never exercised — real long-reply synthesis has never round-tripped, since the cap made it unreachable and tests stub synthesizeFn/convertToOggFn/sendVoiceFn throughout). Bumped frontmatter (sources +bridge/speech.ts, tags +voice/stt/tts) and added a relationship to investigations/2026-07-18-telegram-voice-stt-tts-spec-gaps.md, whose gap-3 block already carries a parallel RESOLVED note. index.md's telegram-frontend line refreshed to mention voice. Verified: npm run typecheck clean, npm test 391/391 (per Gary's task brief — not independently re-run from this wiki session). No new sources page (small diff, capability-section precedent from PR #27). Lint scope: touched content only — no contradictions introduced (chunking-boundary bullet still explicitly text-only, not extended to voice).

## [2026-07-18] ingest | Voice-reply character count (assistant-agent commit `6f409be`, merged directly to `main` — no PR, per Gary's explicit "merge it" instruction): Gary asked for a character count on voice replies after confirming a real over-1000-char reply had gone out correctly following PR #42's cap removal. Built TDD in an isolated worktree (`.claude/worktrees/voice-reply-char-count`): `bridge/api.ts`'s `sendVoice` gained an optional third `caption` param (appended to the multipart form only when given — verified RED then GREEN in `bridge/api.test.ts`); `bridge/telegram-bridge.ts`'s `drainFifo` now computes `${spoken.length} chars` from the same sanitised text handed to `synthesizeFn`, logs it server-side, and passes it to `sendVoiceFn` as the caption — so the count shows in both the bridge log and the Telegram UI next to the voice note, per Gary's "both" answer to where it should surface. Updated capabilities/telegram-frontend.md (two new paragraphs under the Voice section: a note on Gary's live over-1000-char confirmation, and the character-count feature itself), bumped tags (+character-count), index.md's telegram-frontend line refreshed. Verified: npm run typecheck clean, npm test 395/395 (391 baseline + 4 new tests) both pre-merge in the worktree and post-merge on `main`; pushed to origin/main (6f409be); worktree removed, feature branch `worktree-voice-reply-char-count` deleted (unlocked, clean fast-forward merge, no `enforce_pr_workflow` block encountered). No new sources page (small diff, same capability-section precedent as PR #42/#27). Lint scope: touched content only — no contradictions introduced (the pre-existing "real long-reply synthesis is unverified" claim narrowed, not overturned: Gary's live confirmation covers reply delivery, not an explicit end-to-end automated test).

## [2026-07-18] ingest | Untrack personal task files (assistant-agent PR #43, chore/untrack-personal-tasks, merged e27b7cf): `tasks/*.md` gitignored — personal task files (`launch-model-routing-loop.md`, `salience-cue-experiment-readout.md`) stay on disk but leave the shared repo, so a fresh clone won't have them. Five files stay explicitly tracked despite matching the glob, because untracking them breaks a fresh clone: the three launchd plists (read by `scripts/install.sh`), `inbox-brief.md`, and `proactive-calendar.md` (read by `proactive/sweep.ts`, asserted in `proactive/sweep.test.ts`) — `.gitignore` carries a per-file exception for each. New `tasks/EXAMPLE-task.md` (tracked) documents frontmatter as actually used, correcting capabilities/tasks.md's prior single-shape description: Gary-tasks use `title/status/due/priority`, but loop-launcher task files (`tasks/launch-*.md`) use a separate `title/slug/repo/permission_mode/status` shape that was previously undocumented in this wiki. Also states the non-obvious bit: task files aren't date-triggered, nothing scans the directory. Updated capabilities/tasks.md (new "Frontmatter — two shapes" note, corrected the stale "tracked files" claim, added the five-file tracked-exception list), index.md's tasks line refreshed. Verified (from PR body, not independently re-run this wiki session): npm run typecheck clean, npm test 391/391, `./scripts/install.sh --dry-run` resolves all four services, `git check-ignore` confirmed both directions. No new sources page (small diff, capability-section precedent). Lint scope: touched content only — no contradictions introduced.

## [2026-07-18] ingest | Inbound voice success log (assistant-agent PR #44, worktree-voice-inbound-success-log, merged f31cacf): inbound voice-note handling previously only logged failures (download error, transcribe error) — a successful transcription was silent, so the bridge log had no independent evidence an inbound voice round-trip happened; only the outbound `voice reply: N chars` line existed. Surfaced while verifying Task 10 of the voice-STT-TTS plan (first real end-to-end Telegram check). Fix: mirrored `console.log("[telegram-bridge] voice received: N chars")` line right after a successful `transcribeFn` call, before the transcript is pushed to the FIFO — same character-count convention as the existing outbound line. Updated capabilities/telegram-frontend.md (new paragraph in the Voice section, directly after the Inbound paragraph), index.md's telegram-frontend line refreshed. Verified (from PR body, not independently re-run this wiki session): built TDD (red without the line, green with it), npm run typecheck clean, npm test 396/396 (up from 395 baseline). No new sources page (1-line functional diff, capability-section precedent). Lint scope: touched content only — no contradictions introduced.

## [2026-07-19] ingest | AskUserQuestion hook documentation (assistant-agent worktree-fix+ask-user-question-hook merged ad7dc35): new PreToolUse hook (`gate/askUserQuestionHook.ts`) denies all `AskUserQuestion` tool calls with a deterministic redirect to conversational asking. This hook enforces a hard constraint: Rachel has no host-side renderer for the tool's interactive UI (Telegram, terminal, and headless one-shots are text-only), so when a request is ambiguous or missing context, she must ask via text rather than attempting a tool call. Unit tests in `gate/askUserQuestionHook.test.ts` cover deny-path, pass-through, non-event-type pass-through, and exception handling (4/4 coverage). E3 evaluation suite (evals/e3-ask-before-acting.md) verifies Rachel asks a specific clarifying question instead of guessing (4/4 cases pass as of 2026-07-19). The hook runs as a second PreToolUse parallel to the send gate (gate/sendGate.ts), created once at module scope in rachel.ts. New capabilities/ask-user-question-hook.md documenting the hook's purpose, mechanism, test coverage, and E3 eval results; updated index.md to link the new capability page. No new sources page (one file + tests, fully covered by capability-page framing; follow cabinet-precedent from e.g. send-gate.md ingest). Lint scope: touched index.md + added new capability page — no contradictions introduced (new capability is independent of existing send-gate docs, cross-link added to both), no orphans (new page linked from index, E3 eval framed as evaluation not external source).


## [2026-07-20] ingest | Persistent calendar index (PR #48)

Added cross-session calendar awareness: upcoming (7-14d) and recent (30d) events auto-refreshed to wiki 4x/day. Source page documents the problem, solution, and key decisions.

## [2026-07-20] lint | Frontmatter audit and broken-link cleanup, 28 pages. Fixed 4: (1) capabilities/installation.md — added missing title, type, created/last_updated (2026-07-15), sources; previously had tags only. (2) sources/2026-07-15-installer.md — added title, type, created/last_updated (2026-07-15), sources; previously bare tags only. (3) sources/2026-07-20-persistent-calendar-index.md — replaced non-standard fields (origin, date) with standard frontmatter (created, last_updated = 2026-07-20). (4) capabilities/ask-user-question-hook.md — removed broken wikilink [[patterns/ask-before-proceeding]] (page doesn't exist). Checked and clean: all 28 pages now have consistent frontmatter format (type + created + last_updated + sources + tags, with special pages exempt); 1 stale source (karpathy-llm-wiki.md 43d old, external reference exempt from 90d stale threshold); 0 orphaned pages (all linked from index or special-purpose); 0 broken wikilinks; anchor links verified (e.g. [[#Self-monitoring (PR #21 + #22)]] section exists). Confirmed: email/calendar gaps are sanctioned in index.md ("Not yet documented"); no data contradictions; raw/ directory empty, no inbox backlog.

## [2026-07-20] ingest | Ingest triage rule for actionable backlog (assistant-agent commit `49c478b`, direct to `main`, doc-only precedent). `AGENTS.md`'s Ingest workflow gained a Step 0: classify each `raw/` source before writing anything — reference/knowledge material still becomes wiki pages, but an actionable backlog/TODO list is written instead to `assistant-agent/tasks/*.md`, one file per item, with a `Repo: <path>` body line for items targeting a repo other than `assistant-agent`. Prompted by a same-day incident: today's `raw/task_to_do.md` ingest was nearly double-handled — correctly into `assistant-agent/tasks/` once, and separately as an abandoned, never-committed write into `assistant-agent-wiki/tasks/`, a page type never part of this wiki's documented schema (six types: architecture/capabilities/patterns/investigations/sources/templates). Cleaned up in commit `51bbeb9` (`assistant-agent-wiki`) — that directory is now empty and unused. Light-touch ingest, not a full sweep: updated `capabilities/tasks.md` only (new constraint bullet + sources bump), no new `sources/` page — `index.md` already delegates ingest-workflow documentation to `AGENTS.md` by reference rather than duplicating it in wiki pages, and `AGENTS.md`'s own Evolution section treats itself as self-maintaining, so a full 5-15-page ingest sweep would be documenting the same fact twice. Lint scope: touched content only — no contradictions introduced.

## [2026-07-21] lint | Full-vault lint, 30 non-template pages. Fixed 4, all verified against source this session (not taken from a worker report). (1) **`capabilities/send-gate.md` carried a security-relevant claim now false**: it said the SDK "is fail-*open* on both thrown exceptions and timeout". Commit `9c45b34` (2026-07-21) corrected the project's own `AGENTS.md` record — on `@anthropic-ai/claude-agent-sdk` 0.3.216 the *throw* path is still fail-open (re-verified), but a hook resolving after its declared `HookCallbackMatcher.timeout` now fails **CLOSED** (1s declared vs 8s hook → blocked), where the 0.2-era SDK let the tool proceed. The wiki never followed the code repo's correction. Rewrote the fail-closed bullet to carry the split, the version-dependency caveat, and why both the try/catch (throw path still fail-open) and the internal deny-timer (now defence-in-depth, still settles first) stay load-bearing. Verified against `AGENTS.md:115-120`. (2) **`capabilities/tasks.md:49` asserted five files stay tracked "because untracking them would break a fresh clone" — the code untracked two of them anyway.** Commit `a505042` (2026-07-21, SDK 0.3.216 upgrade + untrack personal task files) removed `tasks/inbox-brief.md` and `tasks/proactive-calendar.md` from git. `git ls-files tasks/` now returns only `.gitkeep`, `EXAMPLE-task.md`, and the three launchd plists; `.gitignore` carries exactly ONE exception line (`!tasks/EXAMPLE-task.md`), not "an explicit exception per file". Corrected the count to three (the plists are tracked because they're `.plist` and `tasks/*.md` never matched them), marked the old claim superseded, and recorded the live gap the change opens: `scripts/install.sh` still installs `com.rachel.inbox-brief` and `com.rachel.proactive-calendar`, whose task files a fresh clone no longer has — **flagged for Gary, not reconciled in code**. (3) `index.md` repeated the same wrong "5 infrastructure files" claim — corrected in the tasks line. (4) `architecture/mcp-integrations.md` cited "rachel.ts line ~92" for the Chrome `extraArgs` wiring; actual line is 191 (off by ~99) — corrected and pinned with a verification date. Re-verified while there: `mcpServers = {}` still true (`rachel.ts:83`), `extraArgs: { "chrome": null }` still present (`rachel.ts:191`). Checked and clean: **0 broken wikilinks** — a naive scan flags 12, but all are false positives or sanctioned: `[[capabilities/email]]`/`[[capabilities/calendar]]` are the standing "Not yet documented." placeholders in `index.md` (kept by two prior lints), `[[#Self-monitoring (PR #21 + #22)]]` is an in-page anchor that resolves (heading exists at `capabilities/telegram-frontend.md:114`), and the rest live in `log.md`, which is append-only history and must never be edited. **0 stale pages** at this project's own 90-day threshold (`AGENTS.md:84`, which overrides the lint skill's 30-day default) — oldest is `sources/karpathy-llm-wiki.md` at 44d, an external reference exempt anyway. **0 orphans** (all reachable from `index.md`; `log.md`/templates exempt). Frontmatter consistent across all typed pages. `raw/` holds one file (`task_to_do.md`), already triaged to `assistant-agent/tasks/` per the Step 0 rule — not a backlog. **NOT FIXED, surfaced as ingest work** (lint's job is to find these, ingest's to write them): six un-ingested commits — `57cdf56` (#46 turn-level watchdog, which supersedes the drain-stall dedup story in `capabilities/proactive-layer.md` and `capabilities/telegram-frontend.md` — the single highest-value follow-up), `5bf56a2` (#45 skip AskUserQuestion), `f762254` (ask-before-acting ground rule), `a505042` (SDK 0.3.216 bump), `9c45b34`, `b3b0079` (platform-binary pin regression). Plus zero-coverage components: `proactive/modelConfig.ts` (model/effort switching has SHIPPED but `index.md` still frames it as a 2026-07-16 investigation/plan), `gate/auditLog.ts`, `gate/surfaces/terminal.ts`, `gate/bashPatterns.ts`, the `hooks/` directory (contains no runtime hooks — only a bash probe test — which the wiki never states), and the `evals/` harness methodology.

## [2026-07-21] decision | Repo depersonalisation — public repo stripped of operator identity

`blueman82/assistant-agent` is public and several tracked files carried the
operator's name, personal email, absolute home paths, or private artefacts.

Untracked (all still on disk, still read at runtime): `tasks/inbox-brief.md`,
`tasks/proactive-calendar.md`, all of `.claude/`, all of `evals/`,
`workflow.config.yaml`, plus `.DS_Store` and `.obsidian/`.
`workflow.config.yaml` was untracked rather than genericised because
`wiki_path: null` is a live instruction meaning "skip the wiki phases", not a
neutral placeholder.

`prompts/system.md` named the operator 38 times and could not simply be
untracked — `rachel.ts` exits 2 without it. Split instead: the tracked file is
now generic (all 191 lines and every behavioural rule preserved), and the real
prompt moved to a gitignored `prompts/system.local.md`.
`proactive/systemPrompt.ts` resolves between them, mutation-proved, 416/416.

**One removal reverted.** The launchd plists were untracked in the same sweep,
then restored: a clean checkout failed 378/26, every failure in
`scripts/install.test.ts` reading `template missing`. They turned out to be
install machinery carrying no personal data — `install.sh` stamps
`__REPO_PATH__` at install time. Re-tracked via an explicit `!tasks/*-launchd.plist`
negation so the intent is legible rather than implicit.

The lesson: "is this file personal?" and "does the repo need this file?" are
different questions. A clean-checkout test run settled the second; reasoning
about the file's location would not have.

Filed as [[investigations/2026-07-21-repo-depersonalisation]]. Corrected a now-stale
claim in [[capabilities/tasks]] (the plists are tracked deliberately, not
because they escaped a `.md`-only pattern).

## [2026-07-21] ingest | cross-platform persistent memory (PRs #49-#52, one cluster)

New pages: [[sources/2026-07-21-cross-platform-persistent-memory]] (cluster
source), [[capabilities/memory]] (the fact-based store + deterministic prompt
injection), [[investigations/2026-07-21-rejected-shared-session-thread]] (why one
shared session thread was rejected in favour of two separate mechanisms).

Corrected a stale claim in [[architecture/overview]]'s Session model: "one
long-lived process, one session... until /reset or process exit" no longer
holds for the Telegram bridge specifically, which now persists sessionId
across a process restart (PR #51/#52). Added the `RACHEL_MEMORY_PATH` and
`RACHEL_SESSION_FILE` env seams to its Config table. Added a session-
continuity note to [[capabilities/telegram-frontend]]. Updated index.md
across Architecture/Capabilities/Decisions/Sources sections.

Note: Step 3 (discuss key takeaways with the user) was not performed
interactively — this ingest ran as a headless loop-boundary duty delegated by
an orchestrating agent with no attached human this turn; `wiki.supervision`
is unset (defaults to `discuss`) in this vault's workflow.config.yaml, so this
is a documented deviation from the skill's default gate, not a silent skip.

## [2026-07-21] lint | Post-ingest lint of the cross-platform persistent memory cluster (PRs #49-#52), 34 non-template pages. Verified against source, not taken on the wiki's own word: `rachel.ts:246` (`prompt: composeSystemPrompt(systemPrompt, resolveMemoryPath())`) matches capabilities/memory.md and sources/2026-07-21-cross-platform-persistent-memory.md exactly; the env-delete fix for the one-writer hole is real at rachel.ts's session-options block (~line 263, confirmed by inspection); PR SHAs confirmed by `git log` (a002085=#49, 7be07d2=#50, 7f8d2a4=#51, f38c074=#52); `.rachel/bridge-session.json` exists on disk at the repo-local (not `~/.rachel/`) path the pages claim, and is gitignored as stated. **0 contradictions** — architecture/overview.md and capabilities/telegram-frontend.md are the only two pages describing session-persistence behaviour and both carry the bridge-restart exception consistently; no other page still states the old "one long-lived process, one session until /reset or exit" claim unqualified. **0 orphans** among the 7 touched/new pages — sources/2026-07-21-cross-platform-persistent-memory.md, capabilities/memory.md, and investigations/2026-07-21-rejected-shared-session-thread.md are all linked from index.md and cross-link each other plus architecture/overview.md and capabilities/telegram-frontend.md. The only orphan in the whole vault remains raw/task_to_do.md, already triaged to assistant-agent/tasks/ per a prior lint — pre-existing, outside this ingest's scope. **0 stale pages** at the project's 90-day threshold (AGENTS.md) — oldest is sources/karpathy-llm-wiki.md at 45 days, an exempt external reference; every page touched by this cluster carries last_updated: 2026-07-21. Frontmatter consistent (title/type/created/last_updated/sources/tags on all three new pages). Not fixed, not this cluster's scope: capabilities/tasks.md's already-flagged live gap (scripts/install.sh still installs launchd jobs for two task files a fresh clone no longer has) is unrelated to memory/session and untouched by this ingest — left as a standing flagged-for-Gary item from the 2026-07-21 full-vault lint.

## [2026-07-21] fix | taxonomy correction: decisions/ eliminated per Gary's ruling

`decisions/` was not a page type AGENTS.md sanctions (architecture/,
capabilities/, patterns/, investigations/, sources/, templates/ only), yet two
tracked pages lived there: [[investigations/2026-07-21-rejected-shared-session-thread]]
(created this cluster) and [[investigations/2026-07-21-repo-depersonalisation]]
(pre-existing, created earlier today). Gary ruled: nothing may live outside the
documented taxonomy. Both moved to `investigations/` via `git mv` (history
preserved), frontmatter `type:` corrected from `decision` to `investigation` on
both, every `[[decisions/...]]` wikilink across the vault rewritten to
`[[investigations/...]]` (index.md, log.md's own prior entries, capabilities/memory.md),
and index.md's separate `## Decisions` section merged into `## Investigations`.

`repo-depersonalisation_2026-07-21.md` was renamed to `2026-07-21-repo-depersonalisation.md`
in the move — investigations/ already enforces a strict `YYYY-MM-DD-slug.md`
prefix (both pre-existing pages follow it), so the old underscore-date suffix
would have been the one non-conforming filename in the directory. Noted here
rather than done silently, per instruction.

Also surveyed: `assets/` (empty, 0 tracked) and `logs/` (1 untracked hook
artifact, 0 tracked) are technically off-taxonomy directories too, but neither
pollutes the tracked vault — left alone, not this fix's concern.

Verified zero remaining `decisions/` references vault-wide via
`grep -rn "decisions/" --include="*.md" .` returning nothing.

## [2026-07-21] lint | Re-lint after decisions/ taxonomy fix, 27 non-template pages

Re-verified the taxonomy correction landed clean. Both moved pages —
[[investigations/2026-07-21-rejected-shared-session-thread]] and
[[investigations/2026-07-21-repo-depersonalisation]] — carry `type: investigation`
in frontmatter and are reachable from index.md's `## Investigations` section
(no separate `## Decisions` section remains). `decisions/` directory does not
exist. Zero remaining `[[decisions/...]]` wikilinks or `type: decision`
frontmatter anywhere; the only surviving text mentioning "decisions/" is this
log's own prior entry describing the fix (historical record, not a live
reference), plus two unrelated plain-English uses ("Key Decisions" heading in
sources/2026-07-20-persistent-calendar-index.md, "replace-not-add decision" in
index.md's slack line) — neither is a taxonomy reference.

**Fixed 1**: capabilities/tasks.md's `[[repo-depersonalisation_2026-07-21]]`
link never resolved — the taxonomy fix's `git mv` renamed that file from
`repo-depersonalisation_2026-07-21.md` to `2026-07-21-repo-depersonalisation.md`
(documented in the fix's own log entry above) but this one inbound link outside
investigations/ wasn't updated to match. Corrected to
`[[investigations/2026-07-21-repo-depersonalisation]]`.

**0 contradictions** — no ⚠️ CONTRADICTION flags anywhere; no stale `type:
decision` frontmatter; no page presents the old decisions/ taxonomy as current.
**0 orphans** — every non-template, non-index/log page is linked from
index.md; the only orphan in the vault, raw/task_to_do.md, is pre-existing,
already triaged to assistant-agent/tasks/, and outside a wiki page (not subject
to the index-linkage check). **0 stale pages** at the 90-day threshold —
oldest is sources/karpathy-llm-wiki.md at 44 days (exempt external reference);
both moved pages carry last_updated: 2026-07-21. raw/ has no untriaged
backlog beyond the already-known task_to_do.md.

## [2026-07-22] ingest | Telegram PDF ingestion (assistant-agent PR #54, feature/telegram-pdf-ingestion, merged `6e96a41`; post-review bugfix commit `197a14f` same day). Rachel can now receive PDF documents via Telegram, alongside the existing photo/image-document path (PR #17): the document handler's mime check now also accepts `application/pdf` (tracked via a new `isPdf` boolean), sharing all download/temp-file mechanics with images but queuing to `runTurn` under a distinct FIFO tag, `[document: /path]`, rather than `[image: /path]` — a PDF is read via the `Read` tool's native PDF support, not vision, so `prompts/system.md` gained a parallel "Receiving PDFs from Telegram" section instructing Rachel accordingly. `197a14f` fixed a bug the code-reviewer caught during PR #54's own review: a dot-less document filename (no extension) fell back to a hardcoded `.jpg`, which for a PDF meant PDF bytes saved under an image extension that `Read` would fail to decode — fixed by making the fallback type-aware (`isPdf ? "pdf" : "jpg"`). This reverses an earlier decision: PR #17 (2026-07-08) explicitly dropped PDF support as "unasked-for scope" per CLAUDE.md's YAGNI rule; Gary this time explicitly asked for it, so the earlier exclusion's basis no longer holds. Updated capabilities/telegram-frontend.md (new "Image and PDF reception" section text, FIFO-format and known-debt lines extended to cover PDFs, frontmatter bumped); added a supersession note (not a rewrite) to sources/2026-07-08-telegram-image-reception.md's "PDF scope dropped" bullet, cross-linking forward to this PR; new sources/2026-07-22-telegram-pdf-ingestion.md; index.md's telegram-frontend and sources lines refreshed. Known debt (file-size cap, temp-file cleanup) is pre-existing and shared with the image path — noted as now-also-applying-to-PDFs, not re-opened as new debt. Verified against source this session: `bridge/telegram-bridge.ts` lines 622-674 (isPdf, tag const, FIFO push), `prompts/system.md` lines 77-83 (document-tag contract), CLAUDE.md's architecture bullet — all three confirmed to match actual merged code, no drift found. Lint scope: touched content only — no contradictions introduced (the PR #17 page's historical claim is preserved, only annotated; no other page still states PDF support as excluded).

## [2026-07-22] ingest | assistant-agent PR #55 — voice-reply synthesis timeout

Voice replies fell back to text on long input. Root cause: `SYNTHESIZE_TIMEOUT_MS` was a
flat 20s while real synthesis of a 9696-char reply takes ~30s, so `execFile` killed the
child. Fixed by scaling the budget with text length rather than bumping the constant.
Also captured child stdout in the thrown error — mlx-audio prints its failure reason to
stdout, which `synthesize()` was discarding, making the failure read as a bare progress bar.

Pages: new [[sources/2026-07-22-pr55-voice-synthesis-timeout]]; updated
[[capabilities/telegram-frontend]] (timeout contract + closed the unverified-long-reply gap),
[[investigations/2026-07-18-telegram-voice-stt-tts-spec-gaps]] (gaps 3 and 6 resolved from
the other direction), `index.md`.
